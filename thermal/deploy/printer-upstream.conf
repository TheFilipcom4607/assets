# Auto-managed by find-printer.sh — do not edit by hand.
# Holds the printer's current address so nginx can proxy to it. The placeholder
# below just lets nginx start before the printer has ever connected; discovery
# overwrites it with the real address (and reloads nginx) once it appears.
upstream xiao { server 192.168.50.2:80; }
